DATABASE_URL=sqlite://persistent-agent.db?mode=rwc
BIND_ADDR=127.0.0.1:8080
SCHEDULER_INTERVAL_SECONDS=30

# Optional. When unset, the server uses the deterministic stub worker.
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-chat
